import { PoseMap, TransitionFactory } from '../types';
declare const generateTransitions: <A>(poses: PoseMap<A>, defaultTransitions: Map<string, TransitionFactory<A>>) => PoseMap<A>;
export default generateTransitions;
