import { Pose, PoseMap, PoserConfig, SelectValueToRead } from '../types';
export declare const getPoseValues: <A>({ transition, delay, delayChildren, staggerChildren, staggerDirection, afterChildren, beforeChildren, preTransition, ...props }: Pose<A>) => Pose<A>;
export declare const selectPoses: <V, A>({ label, props, values, parentValues, ancestorValues, onChange, passive, initialPose, ...poses }: PoserConfig<V>) => PoseMap<A>;
export declare const selectAllValues: <V>(values: Map<string, V>, selectValue: SelectValueToRead<V>) => {
    [key: string]: any;
};
