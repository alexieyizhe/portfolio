import { Transition } from '../types';
declare type TransitionMap = {
    [key: string]: Transition;
};
export declare const eachValue: (map: TransitionMap) => Transition;
export declare const fromPose: (map: TransitionMap) => Transition;
export {};
