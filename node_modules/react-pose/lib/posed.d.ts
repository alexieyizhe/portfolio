import * as React from 'react';
import { ReactElement } from 'react';
import { PoseElementProps } from './components/PoseElement.types';
import { DomPopmotionConfig } from 'popmotion-pose/lib/types';
export declare type ComponentFactory = (poseConfig?: DomPopmotionConfig) => (props: PoseElementProps) => ReactElement<any>;
export declare type Posed = {
    (component: React.Component): ComponentFactory;
    [key: string]: ComponentFactory;
};
declare const posed: Posed;
export default posed;
