import * as React from 'react';
import { ReactElement } from 'react';
import { Props, State } from '../components/PoseGroup';
declare type MergeChildrenProps = Props & State & {
    displayedChildren: Array<ReactElement<any>>;
};
export declare const handleIncomingChildren: (props: MergeChildrenProps) => React.ReactElement<any>[];
export declare const makeChildList: (children: React.ReactElement<any>[]) => React.ReactElement<any>[];
export declare const removeFromChildren: (children: React.ReactElement<any>[], key: string) => React.ReactElement<any>[];
export {};
