name: Refresh data
on:
  schedule:
    - cron: '0 * * * *'
jobs:
  refresh-data:
    runs-on: ubuntu-latest
    steps:
      - name: Refresh
        run: curl https://alexxie.com
        run: curl https://alex.xie.codes
        run: curl https://xie.codes
        run: curl https://alexxie.ca
